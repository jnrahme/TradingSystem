# Policy SDK

Defines contracts for regime models, ranking models, parameter policies, and approved policy outputs.

Strategies can consume approved policy results through this SDK, but policy code must still emit strategy-safe outputs rather than raw broker actions.

