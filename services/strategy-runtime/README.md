# Strategy Runtime

Loads strategy plugins, provides read-only runtime context, persists strategy state snapshots, and collects strategy intents.

This is the boundary that keeps strategy code isolated from the rest of the platform.

